# snake-tokscale environment file (template)
# Copy to `.env` locally if you need private overrides:
#   cp env.example .env
#
# No secrets are required — the tokscale.ai API is public.
# All non-secret configuration lives in `config.toml`.
#
# Placeholder for future private values:
# TOKSCALE_API_TOKEN=
